{
  "name": "@proficient/client",
  "version": "0.9.3",
  "license": "MIT",
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**",
    "core/**"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:dist",
    "build:core": "rm -rf core && fern generate",
    "build:dist": "rm -rf dist && tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "lint:fix": "TIMING=1 eslint --fix \"src/**/*.ts*\""
  },
  "dependencies": {
    "@proficient/api": "0.22.0",
    "axios": "^1.3.2"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "tsconfig": "*",
    "typescript": "5.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
